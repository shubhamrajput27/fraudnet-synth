# orchestrator/

Phase 5. FastAPI service that unifies the augmentation and federated-training layers behind internal HTTP endpoints called by the `gateway/`. Triggers ML runs and streams round-by-round progress.
