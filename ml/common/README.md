# ml/common/

Shared code used by every arm and phase: the PyTorch MLP model definition (D2), fraud-class precision/recall/F1 metric utilities, config loading, and the global random seed helper. Single source of truth to avoid duplicated training logic across the six arms.
