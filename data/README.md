# data/

Gitignored. Holds the raw ULB Credit Card Fraud dataset, per-client shards, synthetic batches, and the global held-out test split. Nothing here is committed — regenerate via `ml/partition/` scripts using the pinned `--seed`.
