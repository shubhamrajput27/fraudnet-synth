# experiments/

Run configs, collected results, and convergence plots for the six-arm evaluation grid (Phase 7). Each run config records its arm, seed, and hyperparameters so results are reproducible.
