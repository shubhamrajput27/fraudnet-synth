# ml/baselines/

Phase 4. Isolated (each bank trains alone) and Centralized (all data pooled) arms, real-only and augmented, sharing the same model definition and training loop as the federated arm — no forked training logic per arm.
