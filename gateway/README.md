# gateway/

Phase 5. Node.js + Express API gateway: JWT auth, run management, and MongoDB persistence (`runs`, `round_metrics`, `client_metrics`, `synthetic_batches`, `validation_reports` collections per D8). Public-facing API consumed by `dashboard/`.
