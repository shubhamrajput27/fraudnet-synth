# dashboard/

Phase 6. React + Recharts (or Chart.js) UI: mode indicators (Augment/Schema/Real-only), synthetic quality panel, live FL round charts over WebSocket, the six-arm comparison grid, a "test a transaction" demo, run history, and synthetic dataset export.
